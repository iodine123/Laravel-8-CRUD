pipeline{
    agent any
    environment{
        DOCERHUB_CREDENTIALS=credentials('jenkins-build-push')
    }
    stages{
        stage("Build Image"){
            steps{
                echo "========executing A========"
            }
        }
    }
}